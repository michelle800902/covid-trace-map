const initialState = {
    time: {

    },
    base: {

    },
    zoom: {

    },
};

const setToolbar = (state = initialState, action): typeof initialState => {
    switch (action.type) {
        default:
            return state;
    }
};

export default setToolbar;
