const initialState = {
};

const setZoom = (state = initialState, action): typeof initialState => {
    switch (action.type) {
        default:
            return state;
    }
};

export default setZoom;
