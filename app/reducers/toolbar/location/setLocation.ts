const initialState = {
    userLocation: [],
    searchLocation: [],
};

const setLocation = (state = initialState, action): typeof initialState => {
    switch (action.type) {
        default:
            return state;
    }
};

export default setLocation;
